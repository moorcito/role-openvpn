---
- name: Install OpenVPN & EasyRSA
  apt:
          name: openvpn
          state: latest

- name: Copy .ovpn file
  copy:
          src: "files/{{ ansible_hostname }}.ovpn"
          dest: /etc/openvpn/homelab.conf

- name: Autostart OpenVPN connection
  lineinfile:
          path: /etc/default/openvpn
          regexp: '^#AUTOSTART="all"'
          line: 'AUTOSTART="all"'

- name: Restart openvpn service
  systemd:
          name: openvpn
          enabled: true
          state: restarted
